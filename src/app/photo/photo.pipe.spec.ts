/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterByTitle } from './photo.pipe';

describe('PhotoPipe', () => {
  it('create an instance', () => {
    let pipe = new FilterByTitle();
    expect(pipe).toBeTruthy();
  });
});
